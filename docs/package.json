{
  "name": "docs",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "jekyll": "docker run --rm --volume=\"$PWD:/srv/jekyll\" --publish [::1]:4000:4000 jekyll/minimal:stable jekyll serve --config _config.yml,_config.dev.yml",
    "build": "webpack --mode production",
    "start": "concurrently 'npm run serve' 'npm run jekyll'",
    "serve": "webpack serve --mode development",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "leaflet.offline": "file:.."
  },
  "devDependencies": {
    "concurrently": "^6.4.0",
    "webpack": "^5.58.2",
    "webpack-cli": "^4.9.0",
    "webpack-dev-server": "^4.3.1"
  }
}
